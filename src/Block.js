import React, { Component } from 'react';
import './index.css';

class Block extends Component {
    render() {
        return (
            {/* get variable based on previous choice */}       
            <Choices />
        );
    }
}